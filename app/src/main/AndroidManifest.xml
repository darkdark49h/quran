<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rounded"
        android:roundIcon="@mipmap/ic_launcher_rounded"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <!-- 1. هادي ولات الرئيسية: PrayerActivity -->
        <activity
            android:name=".PrayerActivity"
            android:exported="true"
            android:theme="@style/Theme.App.BlackSplash"
            android:configChanges="orientation|screenSize|keyboard"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 2. هادي ديال المصحف: MainActivity -->
        <activity 
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.App.BlackSplash"
            android:configChanges="orientation|screenSize|keyboard"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".BookmarksActivity" />
    </application>
</manifest>